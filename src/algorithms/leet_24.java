package algorithms;

public class leet_24 {

    /**
     *         ListNode h = head;
     *         while (h.next != null){
     *             ListNode next = h.next;
     *             ListNode t = next.next;
     *             h.next = next.next;
     *             next.next = h;
     *             h = t;
     *         }
     *         return head;
     */
}
